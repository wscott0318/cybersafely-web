query children($page: Page, $order: UserOrder, $search: String) {
  children(page: $page, order: $order, search: $search) {
    page {
      index
      count
      total
    }
    nodes {
      id
      email
      name
      childRole {
        relation
      }
      roles {
        role
        status
        ... on TeamRole {
          team {
            id
            name
          }
        }
      }
    }
  }
}
