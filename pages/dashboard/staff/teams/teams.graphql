query teams($page: Page, $order: TeamOrder, $search: String) {
  teams(page: $page, order: $order, search: $search) {
    page {
      index
      count
      total
    }
    nodes {
      id
      createdAt
      name
      memberCount
    }
  }
}
